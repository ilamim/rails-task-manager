<h1><%= @task.title %></h1>
<p> <%= @task.details %></p>
<p><%= @task.completed %></p>
<p><%= link_to 'Edit ✏️', edit_task_path(@task) %></p>
<p><%= link_to 'Delete 🗑️', task_path(@task), data: {turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this task?' }  %></p>
<p><%= link_to 'back', tasks_path %></p>
